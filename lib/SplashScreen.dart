import 'package:flutter/material.dart';

//TODO Create this screen for the application splashScreen

class SplashScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Container(

      ),
    );
  }
}
